
```java
public class App {
    static User admin = new User("admin");

    public void process() {
        Order order = new Order();
        order.setUser(new User("guest"));
        
        Payment payment = new Payment(order);
        
        new Log("temp");
    }
}
```

---

## 실행 전 — static 초기화

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
```

---

## process() 호출 직후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    C["Stack
    process Frame
    order = null
    payment = null"]
```

---

## Order 생성 후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    C["Stack
    order"] -->|참조| D["Heap
    Order
    user = null"]
```

---

## User guest 생성 후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    C["Stack
    order"] -->|참조| D["Heap
    Order"]
    D -->|user| E["Heap
    User(guest)"]
```

---

## Payment 생성 후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    C["Stack
    order"] -->|참조| D["Heap
    Order"]
    D -->|user| E["Heap
    User(guest)"]
    F["Stack
    payment"] -->|참조| G["Heap
    Payment"]
    G -->|order| D
```

---

## Log temp 생성 후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    C["Stack
    order"] -->|참조| D["Heap
    Order"]
    D -->|user| E["Heap
    User(guest)"]
    F["Stack
    payment"] -->|참조| G["Heap
    Payment"]
    G -->|order| D
    H["Heap
    Log(temp)
    참조 없음"]
```

---

## process() 종료 — Stack Frame 제거

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Heap
    User(admin)"]
    D["Heap
    Order
    도달불가"]
    E["Heap
    User(guest)
    도달불가"]
    G["Heap
    Payment
    도달불가"]
    G -->|order| D
    D -->|user| E
    H["Heap
    Log(temp)
    도달불가"]
```

---

## Minor GC — 마킹

```mermaid
graph LR
    A["GC Root
    static admin"] -->|"✓ 마킹"| B["Heap
    User(admin)"]
    D["Heap
    Order ✗"]
    E["Heap
    User(guest) ✗"]
    G["Heap
    Payment ✗"]
    H["Heap
    Log(temp) ✗"]
```

---

## Minor GC — 수거 후

```mermaid
graph LR
    A["Metaspace
    App.admin"] -->|참조| B["Survivor
    User(admin)
    age = 1"]
    C["Eden
    전체 초기화
    Order / Payment
    User(guest) / Log 수거"]
```

---

## 이후 생애주기 — Old Gen 승격

```mermaid
graph TD
    A["Eden
    User(admin) 생성"] -->|"Minor GC 생존"| B["Survivor
    age 1 ~ 14"]
    B -->|"age 15 도달"| C["Old Gen
    User(admin)"]
    C -->|"App 종료 또는
    static 참조 해제"| D["수거"]
```

---

## 전체 생애주기 요약

```mermaid
graph TD
    A["User(admin)"] -->|"static 참조 유지"| B["Old Gen 승격"]
    B -->|"App 종료까지"| C["유지"]

    D["Order"] -->|"process 종료"| E["도달 불가"]
    E -->|"Minor GC"| F["수거"]

    G["User(guest)"] -->|"Order 수거"| E
    H["Payment"] -->|"process 종료"| E
    I["Log(temp)"] -->|"생성 직후"| E
```

---

