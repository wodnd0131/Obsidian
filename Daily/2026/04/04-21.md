---
date:
  "{ date }":
tags:
  - daily
---
---
#daily 2026/04/04-21


> [!tip] 리셋 원칙
> 실패 없음 — 내일 리셋. 자책 대신 관찰 한 줄만.

---

## 🌅 아침 

- 기상: 9시 기상 / 12시 반 취짐
- 오늘 컨디션: 🟡

**오늘 딱 1개의 목표**
> ✏️ (작을수록 좋음)
> JVM 관련 키워드 정리

---

## ✅ 업무 & 목표

### 오늘 할 것 (3개 이하)
- [x] 복습 방식 정리를 하고싶다 ✅ 2026-04-21
- [ ] JVM 관련 키워드 정리 (==자바는 어떻게 실행되는가==)
- [ ] 테스트 이유, 자바 선택 이유 등 정리해보기

### 진행 중인 것
> 어제부터 이어지는 태스크, 막힌 것, 기다리는 것

1. 계산기 미션 리뷰 정리하며 인상 깊었던 리뷰 포인트.
- calculator.add(a,b)` 와 `a.add(b)` 의 차이점
	- 빈약한 객체 대신 객체에게 상태와 책임을 부여하기
- 절차 노출로 인한 강한 결합도 문제
	- 인터페이스를 통해 접근하도록 내부 로직을 숨긴다.
	- ex. 너무 많은 티키타가는 절차노출
- Private 메서드는 테스트해야하나? -> 이거 잘 기억안나네
	- 안함, 요구된 기능 -  인터페이스 요소를 테스트한다(*public)
	- private 테스트는 번거로운점이 많고, 응집도를 해친다. 
- 변수의 의미를 이해하는데 코드 몇줄 읽어야하지?
	- 50줄  읽고 이해됬다는 것처럼 이유를 설명해보자
- 함수의 반환 형태를 통일시켜 예상 가능하게 바꾸어라 (메서드 명 단위)
	- parser인데 반환되거나 전달 되는 형태가 동일해야 이해하기 쉽지 않겠나
- ==자바는 어떻게 실행되는가==

정적 팩토리 메서드
	특별한 경우 사용 , 기본 생성자로 의미 전달이 어려울때( 텔레스코핑 문제
		상속을 막는 용도(컴포지션 지향)을 위해 사용될 수 있다
	정적 팩토리 클래스가 안티패턴
		ㄴ 서비스 로케이터 - 전역에서 인스턴스 관리, 클래스 내에서 의존성 부여(숨겨진 의존성)
			ㄴ 이와 대비되는 것이 스프링의 ApplicationContext 이건 외부에서 주입
			ㄴ 싱글톤은 전역에서 상태를 하나로 고정, 강한 의존과 교체 불가 문제, enum 과  hodler 패턴
				ㄴ 스프링 싱글톤은 의존성 주입 프레임워크
					ㄴ 컨텍스트마다 싱글톤이 다름, 외부(스프링)이 주입해주기에 괜찮음
	팩토리 패턴은 GoF임
	팩토리패턴 - 상속으로 해결 - 수직적 변경
		추상 팩토리 패턴 - 컴포지션으로 해결 - 수평적 변경
	유틸 클래스 - 빈약한 도메인 객체 - 비즈니스 로직을 제외한다면 괜찮다
	텔레스코핑 생성자-> 빌더
	캐싱 -> flyweight => 캐싱된 객체과 캐싱값을 사용하는 객체를 분리하자.

동등성(`==`)과 동일성(.equal())
InstancseOf 써야 상속에서도 작동 HashCode 도 함께해야한다는 계약
상속시 확장해서 변경하는 것 x 그런 목적이라면 컴포지션이 맞다
JVM 참조 체계 - > Pass by value 값 자체가 이동,, 참조 타입은 주소를 저장한다
Primitive 원시타입 자주쓰는 값을 heap에 올리는 과정에서 최적화를 위함
 heap에는 값 외에도 gc와 padding등뭐가많음

---
해봄직 한거
- **JVM Heap 구조** — Young Gen / Old Gen / Metaspace. 어제 "heap에는 GC, padding 등 뭐가 많음"의 실체
- **객체 레이아웃 (Object Header)** — mark word, klass pointer. equals/hashCode의 기본 구현이 왜 주소 기반인지 여기서 설명됨
- **GC Roots & Reachability** — 언제 객체가 수거되는가. Flyweight 캐싱이 GC에 어떻게 영향받는지
- WeakReference / SoftReference
- `final` 필드가 JIT에서 어떻게 취급되는지. 어제 Immutable 맥락 연장
### 오늘 실제로 한 것
> 마무리 단계에 채우기 — 계획과 달라도 OK

-  어제 학습 내용 돌아보기, 빈약한 기억 보충하기
	-  Private 메서드는 테스트해야하나? -> 만약 필요하다면 클래스 분리가 맞는게..?

- Entity는 가변적이어야 하는가? - 통제된 가변, 인프라적 제약

- JVM은 어떻게 구성되어있지?

Weak Generational Hypothesis
	JVM GC의 기본 전제, 대부분의 객체는 일찍 죽는다.
Thread-Local Allocation Buffer (TLAB)
	 Eden 내 스레드별 구획 분리, 객체 생성 시 CAS 경합 방지, GC와 목적에서 달라 무관
interpreter
		실행 시 한 줄씩 번역
		이식성 높지만 성능 낮음 (매번 한줄씩 새로 번역), 동적 실행, 즉시 실행
compiler & JIT
		고소준을 번역하여 실행
		정적 실행, 기계어 번역으로 이식성 낮음
		JIT는 바이트 코드로 번역수준 조절, 이식성 높음
		인터프리터 기반으로 작업 hotstop 포인트에 대해 컴파일 최적화
			콜드스타트 문제
GC
도달 가능성으로 수거 판단
old - MSC / young - Copying
STW 줄이기 -> Parallel GC
G1GC는 전체 스캔 방지를 목적으로 GC 영역을 나누기 에 초점을 맞춤. 어떻게 G1여부는 판단하는거지?


JVM
클래스 로더, 런타임 데이터 영역(메모리), 실행엔진(인터프리터,JIT,gc), JNI

javac
자바 소스 코드를 바이트 코드(.class)로 번역




단편화의 의미
Minor GC
**Major GC (Full GC)**
G1GC는 전체 스캔 방지를 목적으로 GC 영역을 나누기 에 초점을 맞춤. 어떻게 G1여부는 판단하는거지?

**PermGen(Permanent Generation)** 이 뭔지 간단한 설명과 PermGen의 문제 좀더 자세히

**Native Memory** (OS가 관리하는 힙 바깥)은 뭐지?
Metaspace는 실질적인 값이 저장되는 곳인가..?(참조 타입이 가리키는 주소에 있는 실제값들??)

LRU 캐시는 뭐야

 **finalize() 에 의존한 자원 해제** -> `try-with-resources` 또는 `Cleaner` (Java 9+)를 써야 한다.
ㄴ 그럼 finalize는 최신 자바코드에선 어떻게 해결하는거지? 이게 바뀌게된 이유 좀더 명확히 설명해줘

실제 프로세스 메모리 ≈ -Xmx + Metaspace + Code Cache + 스레드 스택 + OS overhead
이거 추가설명, 일단 이 배경은 **Metaspace는 Native Memory를 먹는다** 이거 떄문인거지?